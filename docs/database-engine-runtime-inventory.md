# Database-engine migration inventory

Status: M0 authoritative inventory complete for
[database-engine-execution-plan.md](database-engine-execution-plan.md). Last reconciled against the
worktree, reachable history, local artifacts, and `origin/main` initially on 2026-09-17, with the
implementation/evidence rows refreshed through 2026-09-23.

Baseline `b1722b7` remains the comparison point; the snapshot and evidence below describe the current
worktree. This is an implementation and deletion checklist, not a statement that the current
architecture satisfies the database-engine target. Re-open M0 if a new serving path, mutation
mechanism, runtime dependency, package surface, or retained legacy capability is discovered.

## Current-state snapshot

| Item | Authoritative observation | Consequence |
| --- | --- | --- |
| Parent repository | `origin` fetch/push is `https://github.com/rbilleci/titan-graphql.git`. Remote `main` and local `origin/main` are `b1722b7`; local `main` is `9edeb5e`, one commit ahead. | The project is already at the requested GitHub home. Final reconciliation must preserve the current remote tip and publish a reviewed replacement history/commit only after the release gates. |
| Parent worktree | 51 modified status entries and 38 untracked entries after the coded-error increment. Most of the new database engine, standalone frontend, tests, model records, and artifact identity sources are still untracked. | No clean-clone or release claim is possible from the current parent commit. All intended new files must be classified before the clean check-in. |
| Titan pin | Parent records `vendor/titan` at `586c338`; its worktree has 34 modified entries and one untracked regression test. | Required compiler/install/runtime gaps are not reproducible from the recorded submodule commit. They must be tested, committed in `rbilleci/titan`, pushed, and then repinned here before M5/M7. |
| Titan DSL pin | Parent records `vendor/titan-dsl` at `bc759a7`; its worktree has two untracked `Text` source/test files. | Decide from reachability whether this helper is required. Commit and repin it only if generated engine source actually depends on it; otherwise remove it before release. |
| Generated artifacts | The standalone ZIP and four demo/commerce PostgreSQL/MySQL proof package trees are present under ignored `build/`. The ZIP contains only three frontend classes and their nested records plus Jackson and the two JDBC drivers (and MySQL's protobuf dependency). | These are useful local evidence, not source deliverables. Rebuild them from a clean clone; never add `build/` or `.gradle/` to the clean check-in. |
| Latest installed evidence | Current PostgreSQL/MySQL demo packages each have 444 reachable helpers and 441 entry points; MySQL has 445 whole-request routines. Both install from scratch. Focused live dialect methods prove bounded Boolean DNF, computed/relation predicates, computed/relation page/count/boundary ordering, dotted-path cursors, filter-plus-order, policies, and live-row behavior. The previous Commerce policy/read, 65-parent batching, 34-case corpus, and 646-test Docker-free gates remain the last complete broad baseline. | The filter/order compatibility increment is complete. M3 remains open for snapshot/cross-tenant and physically renamed-model proofs, followed by one consolidated final dual gate. Package replacement and release-candidate HTTP evidence remain M5 work. |
| Public routine contract | The v1 source, documentation, runtime diagnostics, unit tests, generated PostgreSQL/MySQL manifests, and deployment descriptors now agree on nine logical inputs after `Connection`: query, operation name, variables, extensions, trusted context, mutation permission, model hash, runtime identity, and package identity. Each generated manifest exposes exactly one `execute_graphql_request` entry point with those nine SQL parameters. | `M1-CONTRACT` is complete as of 2026-09-17. Any incompatible signature change requires a deliberately versioned contract and fresh dual-dialect manifest/installed-package evidence. |
| Privacy/history scan | Reachable history has 26 commits. Author emails are GitHub noreply addresses; one historical commit uses the public `rbilleci` noreply identity. Current and reachable-history scans found no workstation paths or high-confidence credential/private-key patterns. | No current secret/path finding, but the user-requested clean-history publication remains an M7 operation. Re-run scanners over the final index and replacement history. |
| Temporary-file scan | Three untracked heap dumps produced by constrained-heap failures were identified and removed on 2026-09-23: `java_pid3104153.hprof` (968,882,352 bytes), `java_pid3383660.hprof` (975,644,446), and `java_pid3346126.hprof` (963,159,699). The orphaned 512 MiB Gradle daemon that produced the latter was terminated. Heap dumps may contain process/request memory and are never release material. No other reviewed temp/backup/log/editor pattern was found outside ignored `build/`, `.gradle/`, and submodules. | Re-run after staging the clean release tree. Existing ignored build output is local evidence only. |

## M0 requirement-to-evidence matrix

`Status` describes the implementation/evidence state, not whether the subject has merely been
mentioned. Each row names the next acceptance evidence and the legacy removal it eventually unlocks.

| ID | Requirement and current evidence | Status | Next acceptance evidence | Deletion/replacement dependency |
| --- | --- | --- | --- | --- |
| `M0-ROUTES` | All production GraphQL HTTP surfaces are mapped: application `/graphql`, preview `/preview/{previewBuildId}/graphql`, and management `/admin/graphql`. Source inspection confirms preview and management still call the JVM `GraphqlEngine`. | Complete inventory; implementation partial | Add an architecture test enumerating JAX-RS GraphQL paths and their runtime targets so a new semantic route cannot escape the inventory. | Preview/management JVM routes cannot be deleted until retained capabilities have database packages or are explicitly removed from product scope. |
| `M0-MODES` | Application mode selector exposes `java`, `jdbc`, `compiled`, `sql`, and `database`. Serving defaults to and permits only `database` unless a test/reference opt-in is set, but all legacy implementations still compile in `main`. | Complete inventory; implementation partial | Make the release artifact—not the transitional Quarkus application—the only documented/deployable server; prove no alternate mode exists in that artifact. | Unlocks `GraphqlExecutionEngine`, registry, SQL/JDBC/compiled/demo mode deletion after parity. |
| `M1-BOUNDARY` | `databaseEngine`, `databaseFrontend`, and `databaseHttpFrontend` are separate source sets. On 2026-09-17, the engine/frontend/HTTP boundary tasks, exact release-ZIP closure, one-call client test, and fresh isolated PostgreSQL/MySQL frontend integration all passed. The ZIP contains only `io.titan.graphql.frontend` classes and reviewed JSON/JDBC libraries. | Complete (2026-09-17) | Preserve these gates while M2–M5 expand the package; rerun them from the release candidate and clean clone. | The transitional Quarkus route remains a deliberate M6 deletion target after semantic parity, but it is absent from the deployable frontend artifact. |
| `M1-CONTRACT` | One whole-envelope call, transaction outcome framing, model/runtime/package identity inputs, and manifest-selected routine use one v1 nine-input contract. Focused runtime/source-generation tests and both generated manifests assert the exact signature; `DatabaseWholeRequestClientTest` covers the single JDBC invocation. MySQL owns exactly one call per request transaction, applies the database outcome, and invokes `COM_RESET_CONNECTION` only after that transaction ends; serial mutation roots execute inside the one call. | Complete (2026-09-17) | Keep the contract frozen while running the fresh standalone and installed-package gates; treat any incompatible change as v2. | Contract no longer blocks replacement work; legacy SQL dispatch still requires M2–M5 parity before deletion. |
| `M2-LANGUAGE` | The bounded transpiled lexer/typed AST owns selected-operation and fragment closure, built-in and model-registered conditional directives, schema-wide structural and argument validation, recursive variable/literal/default materialization, canonical `ma1`/`cv1` arguments, field collection, duplicate-response-key rejection, coded error/null completion, introspection, and request/deadline bounds for the supported schema. Generated metadata drives Query/Mutation, Relay wrappers, scalars, declared enums, generated/authored input objects, authored interface/union point and static-projection Relay outputs, executable-directive behavior/location validation, directive introspection, and general field-location defaults without an HTTP/JVM semantic path. Abstract selections are validated against declared and possible runtime types; concrete condition sets execute through point, relation, and Relay-node planners. Canonical `fm1` metadata drives object/interface field descriptions, deprecations, and `includeDeprecated` filtering. Every database GraphQL error now carries a source-selected stable code; lexical failures retain bounded locations and execution failures retain paths. Fresh packages pass the 34-case portable corpus on both dialects; the complete JVM suite passes 636/636. Topology is 386 source-local helpers/383 emitted entry points per dialect and 387 MySQL whole-request routines. The v1alpha1 schema is closed explicitly: roots bind one physical projection, unknown root fields are rejected by the public schema, and metadata not authorable in v1alpha1 is not fabricated by a frontend. | Complete for the v1alpha1 supported surface (2026-09-23) | Preserve this core while M3/M4 expand generated read and mutation bindings; any future heterogeneous root must version the model and define discriminator/order/cursor/policy/count semantics first. | JVM lexer/parser/validator/coercer/introspection copies remain deletion targets until M3–M5 prove every retained serving capability and M6 removes them physically. |
| `M2-BUDGETS` | The multi-dimensional request ledger is specified in `query-contract.md`, covering source/envelope size, lexical/semantic/fragment/input/introspection work, generated statements, decoded rows, static filter arity, response assembly, and deadlines. The installed coercer precharges known fan-out and aggregates independent 128-node validation/materialization allowances. Introspection reserves generated inventory counts before rendering. Every non-identity generated JDBC branch reserves from one 64-statement allowance; fixed-cost generic mutations reject impossible root plans before validation/effects. Point, connection-page, boundary, total-count, mutation, and relation carriers share one 1,000-row allowance. Shared JSON member/item/prepend helpers stop assembly growth at 16,384 characters via a private carrier consumed by generated query/mutation and final transport guards. Exact JVM boundaries, generated shapes, installed overflow/rollback tests, five portable resource cases, and the full dual-dialect corpus pass without changing the 30-second statement timeout. | Complete for the current engine surface (2026-09-23) | Preserve every dimension as M2/M3 add semantics; complete M5 cancellation, isolation, and performance evidence without weakening these ceilings. | The cost ledger no longer blocks semantic-oracle deletion; feature parity and independent corpus coverage still do. |
| `M3-READS` | Unrelated demo/commerce models prove the complete reviewed output/root inventory, scalar/composite/enum point keys, multiple roots, cursors/counts/page flags, local/scalar/enum/computed/one-hop relation filters and reviewed tuple ordering, bounded `and`/`or`/`not`, null propagation, and live-row changes. AST-backed plan identities partition distinct aliases, arguments/defaults, merged selections, locations, and static policies. Fixed 64-slot batches cover `MANY`/`ONE` relations at root and deeper compatible levels plus Relay count/page/boundary/nested-node work, including duplicate non-primary keys. | Partial | Prove snapshot/cross-tenant behavior and a physically renamed model before repeating the final dual gate. | Compiled/JDBC data models remain the oracle only for the remaining isolation/genericity evidence; carrier-only public contracts remain until M5 attests the final internal inventory. |
| `M3-POLICY` | Generated root/type/selected-field/relation/target-type reject policies and generic trusted `contextValues` execute in generated preflight before application SQL. Alias/fragment/abstract field selection, root filter/order/count, relation list/to-one/Relay count-and-edge paths, unselected protected members, and top-level introspection authorization pass on both dialects with stable source-located `AUTHORIZATION_ERROR`. v1alpha1 request rejection deliberately does not fabricate row-index paths or actor-shaped schema redaction. | Complete for the v1alpha1 request-reject policy contract (2026-09-23) | Preserve these guards while completing the remaining read and isolation rows; preview/management policy routes remain M4/M6 scope rather than evidence for application reads. | Unlocks JVM application-read policy enforcement after the remaining M3 read rows and M5 parity evidence permit deletion. |
| `M4-MUTATIONS` | Five model-declared Commerce updates and five JVM management commands are enumerated below. Scalar, declared-enum, and nested authored-input `update` mutations are prevalidated, lock rows, execute serially inside one complete request, and return caller-owned commit/rollback outcomes in both dialect proofs. The new generic binding maps dotted input leaf paths to reviewed columns and proves a nested variable plus a database-applied omitted-field default with rollback. A later-root missing-target failure proves atomic rollback after an earlier root staged a write; successful literal/variable enum writes and the multi-root scalar request prove caller commit. JVM CDI command handlers, audit hooks, preview/admin mutations, and broader contracts remain. | Partial | Prove nullable/write-null semantics, concurrency, durable idempotency/audit/outbox, arbitrary reviewed procedures, payload completion, and registry identity for every retained mutation. | Required before deleting `GraphqlApplicationMutationProvider`, executor/handler/audit runtime, and management mutation path. |
| `M5-ATTESTATION` | Runtime and self-excluded final SQL inventory identities are generated, installed, bound, and checked on each current database request. | Partial | Attest the final public/helper/custom-mutation inventory, reject package replacement on a reused pooled connection, and verify manifest/descriptor/database identity agreement on both dialects. | Unlocks older semantic-hash-only binding and legacy entry-point selectors. |
| `M5-CORPUS` | Portable Commerce corpus currently has 34 fixed cases covering named operation/variables, aliases/multiple roots, fragments, built-in and registered conditional directives, relation reads, variable default exception, fragment reachability, typename/nullables, connection merge, wrapper aliases, declared-enum output/filter/root/connection/mutation inputs, nested/defaulted mutation input, generated point/root-connection/relation/flat-mutation/authored-mutation-input argument defaults and their introspection, enum/directive metadata, interface/union point and abstract Relay execution/introspection, abstract fragments through concrete relation/Relay-node positions, mutually exclusive alias reuse, overlapping-branch conflict rejection, exact source-located validation and parse failures, introspection authorization, unsupported subscription, mutation-domain execution failure codes, known input fan-out, request-wide typed-input accounting, pre-render introspection reservation, and fixed-cost application-statement rejection with rollback. The identical corpus passed through freshly installed PostgreSQL and MySQL routines on 2026-09-23. | Partial | Produce a legacy-capability-to-corpus matrix and add the remaining negative mutation/transaction, policy-location, language/read, and adversarial cases; run it through standalone HTTP as well. | Only then may overlapping legacy tests be removed rather than silently discarded. |
| `M5-PERFORMANCE` | Statement deadlines and bounded row/response work exist; selected introspection and package tests complete within the current timeout. Authenticated opt-in metrics expose the exact generated application-statement and row-budget ledgers without changing ordinary responses. Both 65-parent distinct-key to-many and duplicate-key to-one fixtures are exactly three application statements and 131 row-ledger units on both dialects. | Partial | Capture multiple parent-cardinality points, isolation snapshot behavior, timeout/cancellation, and representative generation/install/request latency and package-size measurements for both dialects. | Required before deleting the compiled batching oracle and declaring production readiness. |
| `M6-LEGACY` | Concrete deletion families are listed below; current release ZIP excludes them, but `main` still compiles and tests all of them. | Unstarted deletion | After M1–M5, delete in dependency order and run source, dependency, class/JAR, task, config, route, and documentation absence checks. | This row is the physical-absence completion gate. |
| `M6-HYGIENE` | Current high-confidence secret/machine-path/temp scans are clean, while the parent and both submodules are dirty and most replacement sources are untracked. | Partial | Review every untracked file, scan the final index/history, normalize documentation, and prove no generated/private/local artifact enters the release commit. | Required before the clean-history/check-in operation. |
| `M7-REMOTE` | All three remotes target the `rbilleci` organization. Parent `main` is one local commit ahead of unchanged remote `main`; dependency work has not been committed or pushed. | Partial | Commit/test/push Titan and any required Titan DSL changes first, repin them, build from a clean clone, reconcile remote tips, then create and push the reviewed clean Titan GraphQL history. | Final step only; no early push can establish completion. |

M0 exit result: the requirement/evidence matrix, route/mode table, current model/mutation/test
inventory, repository/dependency snapshot, and concrete deletion manifest classify every discovered
production path and legacy runtime family. `M1-CONTRACT` and `M1-BOUNDARY` are now proven on the
current tree. The earliest unresolved dependency is `M3-READS`: complete the schema-driven read
matrix, relation batching, policy-plan coverage, and parent-cardinality evidence on PostgreSQL and
MySQL. M0 must be
re-run before M6 and M7 as required by the plan.

## Current schema, mutation, and test inventory

This is the current replacement evidence set, not the eventual support promise. Any retained legacy
case absent from these tables must be added to the corpus or explicitly rejected before M6.

| Model/surface | Current reviewed shapes | Database-engine evidence | Missing classification or proof |
| --- | --- | --- | --- |
| Demo blog | `Article`, `User`, `Comment`; point `article`; Relay `articles`; field/relation policy and context-filter examples | Two direct installed tests per dialect plus standalone HTTP coverage of the generated package | Convert the remaining demo JVM/legacy-SQL conformance cases into independent expected results; classify preview metadata separately. |
| Commerce | Seven object shapes, two authored input objects, one interface, one union, two registered conditional directives, nine point roots, six Relay roots including one interface-node connection, direct and cyclic bounded relations, scalar/computed/declared-enum fields including one deprecated output field, root/type/field/relation policies, scalar and declared-enum equality/generated filters and field-location defaults, local/custom order, nullable values, and five typed update mutations | 27 PostgreSQL and 30 MySQL installed-package test methods, generator-shape tests, and the 34-case portable corpus; the focused current-package gates include 65-parent/two-chunk batching and aggregate decoded-row rejection on both dialects | Produce a row for every model field/relation/root option and show which test covers it; extend batching beyond the first root-collection to-many slice and add exact statement instrumentation plus remaining negative schema-use cases. |
| Management model | Eleven management object types, ten point roots, two Relay roots, and management artifact metadata | Served only by the JVM management runtime today; its YAML is not a database-engine release proof | Decide which management roots stay GraphQL, generate their database package, and map durable store/control-plane responsibilities. |
| Application mutation model | `renameCustomer(Int,String)`, `renameCustomerWithInput(RenameCustomerInput!)`, `setCustomerActive(Int,Boolean)`, `setCustomerCreditLimit(Int,Float)`, and `setCustomerStatus(Int,CustomerStatus)` are model-declared `update` operations with key binding, policies, and scalar/enum payloads | Serial lock/update, commit/rollback, missing-row, policy, scalar/enum coercion, nested variable/default coercion, literal-versus-variable enum syntax, and payload behavior appear in the focused Commerce dialect suites | Nullable/write-null semantics, concurrency, arbitrary procedures, idempotency/audit/outbox, and package registry attestation remain. |
| Management mutation runtime | `importModelDocument`, `validateModelDraft`, `generateModelArtifacts`, `approveObservedOperation`, and `rejectObservedOperation` use JVM command handlers with authorization, request-id duplicate policy, and attempt/result audit metadata | JVM unit/integration evidence only | M4 must either implement these through a database-resident management package with durable semantics or replace/remove the GraphQL admin plane explicitly. |

### M3 read, relation, and policy capability matrix

`Proven` means generated static SQL was transpiled, installed, and invoked on PostgreSQL and MySQL;
`partial` names the exact supported subset. This matrix is the sequencing authority for M3. A row
does not become complete from a generator assertion alone.

| Capability | Current database-engine status | Required acceptance evidence / next action | Sequence |
| --- | --- | --- | --- |
| Point root keys | Proven for scalar integral, string-backed `ID`, UUID, and one composite declared-enum-plus-scalar root across the Commerce package. | Trace every declared point root to a dialect case; add missing scalar/composite permutations only where the model format promises them. | Preserve |
| Scalar and computed outputs | Proven: the explicit demo/Commerce inventory selects every reviewed scalar, nullable, ID-storage, enum, and static computed output on both installed dialects, including JSON representation and null propagation. | Preserve the inventory while adding computed/relation use in predicates and ordering. | Complete |
| Root Relay page | Proven for bounded first/last, opaque after/before, exact total count, page flags, local scalar/enum filters, bounded 3x3 DNF composition, reviewed local/computed/one-hop relation tuple ordering and filtering, filter-plus-order, dotted relation cursors, aliases, malformed/null cursor cases, and static interface/union projections on both dialects. | Preserve while closing the separate isolation and renamed-model rows. | Complete |
| Unpaginated `MANY` from a point root | Proven for bounded execution and nested traversal, but one parent means no cross-parent scheduling benefit. | Retain as the correctness baseline for list completion, path errors, and the 101st-child sentinel. | Preserve |
| Unpaginated `MANY` directly under a root collection | Proven: fixed 64-parent chunks cross the 65-parent boundary with exact ledgers; non-primary local joins and duplicate parent keys retain response order, parent ownership, and per-parent bounds. | Preserve. | Complete |
| Unpaginated `ONE` relation | Proven for direct root collections: static 64-slot batches preserve duplicate public/private local keys and parent indexes; zero/one/second-row carriers, nullable/non-null completion, and exact three-statement 65-parent execution are implemented. Existing nullable-boundary propagation remains covered by focused dialect tests. | Add explicit live missing-target and >1-target corruption fixtures where database constraints permit; retain bounded per-parent behavior for point roots and distinct aliases. | Preserve |
| Deeper relation levels | Proven: the owner/path/key/activity carrier recursively batches compatible argument-free unpaginated relations within the hop budget. AST field-start plan identities keep aliases, merged child selections, arguments/defaults, locations, and policy scope separate. Both dialects pass 65 `Customer.orders.customer` traversals in five statements/196 ledger units. | Preserve. | Complete |
| Relation Relay connections | Proven: fixed 64-slot page/count/boundary chunks preserve 65 parents, zero-child parents, forward/backward order, exact counts, aliases, opaque cursors, page flags, distinct plans, and nested node relations on PostgreSQL/MySQL with exact request ledgers. | Preserve. | Complete |
| Argument-bearing relations | Proven: identical reviewed equality plans batch, while distinct literal/variable materialized argument sets are separate AST-backed plan partitions across page/count/boundary work. | Preserve. | Complete |
| Repeated aliases / differing child selections | Proven: compatible fields merge; distinct response aliases/selections become separate plan partitions without per-parent reads and preserve response-key/path ownership. | Preserve. | Complete |
| Root/type policies | Proven on point and Relay page/count/filter/order/alias requests; generated preflight rejects before application SQL. | Preserve. | Complete |
| Field policy | Proven for alias/fragment/abstract-type selection and unselected protected fields; errors retain the selected source location. | Preserve. | Complete |
| Relation policy | Proven on point, batched list, to-one, nested, and Relay edge/count selections. v1alpha1 is request rejection before row execution, so it intentionally reports the selected relation location without fabricating a parent row index. | Preserve. | Complete |
| Introspection policy | Proven as one trusted top-level authorization gate. v1alpha1 deliberately does not claim actor-shaped schema redaction. | Preserve. | Complete |
| Execution work observability | Proven: authenticated `includeExecutionMetrics` adds `extensions.titanExecution` only to completed operations; normal responses remain unchanged. The 65-parent request is exactly three generated application statements and 131 row-ledger units on each dialect. | Retain the diagnostic for scaling tests; remember the row value includes conservative pre-reservations and is not physical database telemetry. | Preserve |

Batching order is intentional: unpaginated to-one and deeper list reads share the existing scalar
carrier and have no cursor/count contract. Relation connections and argument-bearing aliases are
later because a superficially shared SQL statement can be semantically wrong when per-parent
ordering, counts, cursor windows, arguments, policy scope, or error paths differ.

| Evidence family | Current count/scope | What it proves | What it does not prove |
| --- | --- | --- | --- |
| Shared database-core unit tests | 82 tests across whole-request framing, AST, language, type reference, selection plan, input descriptor, and error helpers | Bounded scalar representations, lexical failure offsets, typed error categories, bounded opt-in metric assembly, and many language primitives before transpilation | SQL lowering/deployment or end-to-end schema semantics |
| Whole-request adapter unit tests | Four transitional runtime tests and one isolated-client test | One manifest-selected call, fixed framing, identity binding, and transaction handling at the JDBC boundary | Installed routine correctness or isolated HTTP packaging |
| Generator tests | Eight source-generation tests | Static SQL/source shape, boundary, generated contract assertions, and canonical enum-order identity | Deployability or live dialect behavior |
| Demo installed-package tests | Two PostgreSQL and two MySQL tests | Unrelated minimal model package install/invocation and transitional route binding | Commerce breadth or complete GraphQL semantics |
| Commerce installed-package tests | 27 PostgreSQL and 30 MySQL methods; individual methods contain multiple positive/negative requests | Broad direct-routine behavior including introspection self-description, authored nested input/defaults, declared enum output/mutation/point/connection inputs and generated filters, null propagation, relations/connections, policies, mutation, coercion, directives, budgets, final-response classification, early input-fan-out rejection, exact statement/row-ledger accounting, and corpus execution | A fully automated row-level traceability report; identical method counts do not imply identical cases; a fresh complete post-increment run and final performance/isolation evidence remain |
| Standalone HTTP tests | Two live dialect tests plus one isolated client test | Extracted ZIP can forward complete envelopes to PostgreSQL/MySQL without application classes | Full portable corpus over HTTP, deployment hardening, TLS/auth gateway integration, or release-candidate reproducibility |
| Portable expected-result corpus | 34 Commerce cases, executed by direct installed-package tests on both dialects | Runtime-independent expected JSON for a useful language/read/mutation subset, including registered conditional directive execution/introspection, declared enum behavior, point/root/relation connections, generated field/mutation-argument-default execution, authored mutation-input argument-default composition, introspection, nested/defaulted mutation input, enum metadata, interface/union point and static-projection Relay output, fragments at concrete nested object positions, per-runtime-type alias merging, stable error categories, lexical locations, authorization/unsupported-operation failures, early input-fan-out rejection, request-wide input accounting, pre-render introspection reservation, fixed-cost statement rejection, and exact source-located invalid selection/fragment errors | Negative mutation transaction behavior, remaining read/policy features, other adversarial performance/isolation dimensions, and management/preview capabilities |

The current Commerce package manifests each expose exactly one public entry point. Their generated
artifact parameter list contains `Connection` plus nine SQL-visible inputs. The freshly generated
package sources contain 404 SQL files per dialect (403 transpiler artifact records plus the package
identity migration). Those counts describe the current
proof artifacts only; they are neither limits nor final attestation until reproduced from the release
candidate and reconciled with the custom-mutation registry.

## Concrete legacy deletion manifest

The deletion unit is a runtime capability, not just one class. Re-run dependency analysis before
each deletion because build-time model/code-generation types may share names with runtime types.

| Family | Current concrete sources/surfaces | Required replacement evidence |
| --- | --- | --- |
| Transitional application route and selector | `GraphqlHttpResource`, `GraphqlExecutionEngine`, `GraphqlRuntimeRegistry`, legacy mode properties, Quarkus serving tests and packaging | M1 standalone HTTP artifact and all M2–M5 parity gates |
| JVM language/execution core | `GraphqlLexer`, `GraphqlParser`, `GraphqlAst`, `GraphqlVariableCoercer`, `GraphqlValidator`, `GraphqlReadPlanner`, `GraphqlIntrospection`, `GraphqlCursorCodec`, `GraphqlJsonWriter`, execution/selection/schema runtime records | M2 corpus plus migrated error/location/path and merge/null tests |
| JVM generic data paths | `GenericJdbcGraphqlRuntime`, `GenericJdbcGraphqlDataModel`, `TitanCompiledGraphqlRuntime`, `TitanCompiledGraphqlDataModel`, runtime filter/plan/projection adapters | M3 feature and batching matrix on both dialects |
| Demo and legacy SQL serving | `DemoBlogGraphql*`, `DemoBlogTitanGraphqlFunctions`, `TitanGraphqlFunctions`, `GraphqlSqlModeRuntime`, `GraphqlSqlEntryPointDispatch`, `GraphqlSqlPackageEntryPoints`, `TitanGraphqlRoutineInvoker`, legacy SQL tasks/packages | M2/M3 corpus parity and M1 versioned package entry contract |
| JVM application mutation dispatch | `GraphqlApplicationMutationProvider`, `GraphqlMutationExecutor`, command handler/result, runtime audit sink/log/event, CDI handlers | M4 durable registry/transaction/idempotency/audit/outbox proof |
| Preview GraphQL execution | `GraphqlPreviewHttpResource`, `GraphqlPreviewRuntimeRouter`, preview candidate JVM data models; retain only explicit build/control-plane jobs that do no GraphQL semantics | A separately package-bound database preview path or an explicit product decision to remove preview GraphQL |
| Management GraphQL execution | `GraphqlAdminHttpResource`, `GraphqlManagementDataModel`, `GraphqlManagementMutationSupport`, `TitanGraphqlManagementRuntime`; retain durable management stores and non-GraphQL orchestration as needed | A database-resident management schema/mutation package or an explicit product decision to replace/remove the GraphQL admin plane |
| Carrier-era public serving | Public generated read carrier entry points and tasks used only by compiled/JDBC orchestration | M3 database-internal access proof plus final M5 routine inventory |
| Legacy tests and documentation | Compiled/JDBC/java/legacy-SQL serving tests, mode documentation, old routine signatures, transitional examples | Every useful case mapped into the portable corpus or explicitly classified as an obsolete quirk |

| Boundary | Current implementation | Current execution location | Target disposition |
| --- | --- | --- | --- |
| `/graphql` POST/GET | `GraphqlHttpResource` -> `GraphqlExecutionEngine` | Standard serving wiring permits only fail-closed `database` mode. A legacy `compiled`, `jdbc`, `java`, or `sql` selection requires the explicit test/reference-only `titan.graphql.allow-legacy-execution-modes=true`; otherwise it returns 503 before GET can invoke its historical JVM parser. Database mode forwards the original document and GET's `allowMutations=false` without GraphQL parsing. | Delete all legacy choices once database feature parity is met; retain only HTTP transport/authenticated context and one package-bound whole-request call. |
| `database` mode | `GraphqlExecutionEngine` -> `DatabaseGraphqlWholeRequestRuntime` | JVM verifies the reviewed model/package binding and generated runtime/package-identity sidecars, requires an explicit dialect, resolves only the manifest-published generated whole-request routine, then makes one JDBC call. The routine checks model, runtime, and installed SQL-inventory identities before parsing or application-data access. PostgreSQL/MySQL JAX-RS-resource integration proofs pass. | Transitional production-shaped cutover seam. Correct the signature-version contradiction, complete mutation-registry identity and pooled package-replacement evidence, then remove every alternate serving runtime. |
| `compiled` mode | `TitanCompiledGraphqlRuntime` -> `GraphqlEngine` -> `TitanCompiledGraphqlDataModel` | JVM parses, validates, plans, evaluates policies, schedules reads/batches, and assembles JSON; database runs generated carriers | Replace with database invocation adapter. Delete JVM GraphQL semantics from serving artifact. |
| `jdbc` mode | `GenericJdbcGraphqlRuntime` -> `GenericJdbcGraphqlDataModel` | JVM GraphQL engine and direct JDBC reads | Delete after portable whole-request tests migrate. |
| `java` mode | `GraphqlRuntimeRegistry` -> `DemoBlogGraphqlRuntime` / fixture model | JVM demo parser/executor and fixture data | Retain only temporary build/test oracle material while migrating; delete serving mode and fixture runtime. |
| `sql` mode | `GraphqlSqlModeRuntime` -> `GraphqlSqlEntryPointDispatch` | Thin JDBC call to a database routine, but routine is hard-coded `DemoBlogTitanGraphqlFunctions` | Reuse the package-inventory invocation pattern; remove demo method identities, fixed context tuple, and legacy mode. |
| Whole-request stored function | `DemoBlogTitanGraphqlFunctions` | Database after transpilation, but hard-coded blog parsing, schema, data, policies, and JSON shapes | Migrate proven parser/rendering tests; delete class. The replacement is generated-schema bindings plus shared transpilable engine source. |
| Generated carrier source | `TitanGraphqlRoutineSourceGenerator` -> `GeneratedTitanGraphqlReads` | Database executes point/page/count/relation carriers; JVM orchestrates them | Retain/adapt as database-internal access helpers. Remove carrier-only public serving contract. |
| Application mutations | `GraphqlApplicationMutationProvider`, `GraphqlMutationExecutor`, CDI handlers/audit hooks | JVM validates/dispatches; handler writes through application connection | Replace with build-time registry and transpilable or installed routine implementations. A narrow model-declared database `update` proof now exists, but does not serve this path. |
| Preview endpoint | `GraphqlPreviewHttpResource` -> `GraphqlPreviewRuntimeRouter` | JVM GraphQL engine over preview candidates | Move retained preview query processing to separately bound database packages; move build/file work to control-plane jobs. |
| Admin endpoint | `GraphqlAdminHttpResource` -> management runtime/data model | JVM GraphQL management mutations and stores | Move retained GraphQL semantics to database engine; preserve management orchestration as explicit control-plane operations. |
| Shared GraphQL semantics | `GraphqlLexer`, `GraphqlParser`, `GraphqlVariableCoercer`, `GraphqlValidator`, `GraphqlReadPlanner`, `GraphqlCursorCodec`, `GraphqlIntrospection`, `GraphqlJsonWriter` | JVM process | Port or replace in the transpilable engine source boundary; remove production JVM copies after migration. |
| Artifact binding | `TitanGraphqlPackageBinding`, metadata/inventory classes, `TitanGraphqlDatabaseRuntimeIdentity`, `TitanGraphqlDatabasePackageIdentity` | JVM verifies package before dispatch; the database-mode public routine checks the caller's expected model hash, embedded source-runtime identity, and database-held final SQL inventory identity on every call before application-data access | Retain all three checks, add the final custom-mutation registry to attestation, and prove stale package replacement on a reused pooled connection. |

## Boundaries to preserve

- HTTP authentication and construction of trusted request context.
- JDBC connection acquisition, explicit transaction commit/rollback, deadline/cancellation, media
  negotiation, response headers, and database-availability mapping.
- Build-time model parsing, validation, schema inference, code generation, artifact inspection, and
  package installation verification.
- Test-only reference/oracle code until independent expected-result tests replace the useful coverage.

## Phase 0 completion evidence

- [x] Baseline commit and pinned Titan/Titan DSL revisions recorded.
- [x] `/graphql`, preview, and admin serving paths mapped.
- [x] Existing database whole-request path distinguished from carrier-backed compiled mode.
- [x] Old production/runtime deletion candidates listed with replacement intent.
- [~] Create separate build-time, transpilable-engine, generated-schema, thin-frontend, and test
      dependency boundaries. An isolated `databaseEngine` source set now contains only the
      transpiled proof and generated schema binding, with a check rejecting framework and JVM
      GraphQL dependencies. Independently generated PostgreSQL/MySQL packages for the demo and
      unrelated commerce models are installed and invoked directly. `databaseFrontendJar` now
      contains a framework-free, manifest-selected one-call JDBC client and
      `titanGraphqlVerifyDatabaseFrontendBoundary` rejects JVM GraphQL execution classes from it.
      `titanGraphqlVerifyApplicationFrontendLink` also proves the transitional Quarkus JAR carries
      that extracted client. The same JAR still contains `GraphqlParser`, so the standalone HTTP
      host/deployable module and legacy-class removal are still required. A separate
      `databaseHttpFrontend` source set now builds `databaseHttpFrontendJar`, containing only
      `io.titan.graphql.frontend` classes. Its boundary task rejects JVM GraphQL execution classes,
      non-frontend Titan classes, framework imports, and `main`-application-output leakage. Its
      PostgreSQL HTTP integration test uses a separate source-set classpath with no `main` output
      and serves only the generated whole-request package.
- [x] Build a standalone distribution and satisfy its production artifact boundary test. The
      descriptor is generated from the verified package binding/manifest, and the extracted
      distribution is launched as a separate process against both PostgreSQL and MySQL. Its
      runtime closure contains only frontend classes, JSON transport libraries, and JDBC drivers;
      it cannot load the application JAR, Quarkus, or a JVM GraphQL implementation.
- [x] Capture a portable fixed expected-result corpus independent of the demo whole-request
      implementation. `database-engine-corpus/commerce-v1.json` is executed as fixed expected
      response JSON through the PostgreSQL and MySQL installed commerce packages; no legacy JVM
      or demo runtime supplies the expected values.
- [~] Prove an explicit database mutation binding without a JVM handler. The commerce-only Phase 1
      proof validates a model-declared `update`, emits serial transpiled dispatch, and returns a
      caller-owned `COMMIT`/`ROLLBACK` transport outcome on PostgreSQL and MySQL. The thin client
      consumes a fixed frame/result column rather than inspecting GraphQL JSON. Its broader
      registry and serving migration remain required.
